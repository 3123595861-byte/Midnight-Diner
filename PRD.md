# 需求文档 (PRD)：《AI 深夜食堂：像素风料理与奇妙评价》

## 1. 项目概述
本项目是一款结合“治愈系剧情解谜”与“夸张颜艺吐槽”的 2D 像素风网页小游戏。玩家将扮演深夜食堂的老板，根据不同客人的故事或模糊要求，搭配食材和厨具进行烹饪。系统将通过大语言模型 (LLM) 和图像生成大模型，动态判定料理合理性、生成带梗的食客评价（JOJO 风格/治愈风格），并实时生成像素风食物图片，最终进行结账评分。资金耗尽则游戏结束。

## 2. 技术栈与框架选型
为了确保网页轻量、易于开发且具有现代化的交互体验，项目采用以下技术栈：
*   **开发语言：** TypeScript (确保严谨的数据结构和开发体验)
*   **核心框架：** Next.js (App Router)
*   **样式方案：** TailwindCSS (用于快速构建响应式和像素风 UI)
*   **UI 组件库：** shadcn/ui (提取基础组件如 Card, ScrollArea, Progress, Toast 进行魔改)
*   **AI 服务接入：**
    *   **LLM (文本逻辑与评价)：** 推荐使用 豆包 (Ark) 或 GLM-4 API (擅长中文角色扮演，遵循 JSON 格式化输出能力强，性价比高)。
    *   **图像生成：** 推荐使用 Flux API 或同等支持像素风 (Pixel Art) 生成的 API。
*   **后端交互：** 使用 Next.js 的 Route Handlers (`app/api/...`) 封装 AI 调用，避免前端暴露 API Key。

## 3. 核心游玩流程 (Game Loop)
1.  **开局/新的一天：** 游戏显示当前天数和玩家剩余资金。
2.  **客人点单：** 客人（位于屏幕右上角）出现，在对话框中讲述一段故事或要求。
3.  **烹饪阶段：**
    *   玩家（位于屏幕左下角）在界面下方的“操作区”选择食材（支持多选）和厨具（单选）。
    *   点击【开始烹饪】按钮。
4.  **AI 处理与等待 (Loading UX)：** 
    *   前端进入等待状态，展示“魔性颠勺”像素动画。
    *   服务端并行或串行调用大模型：先获取文本评价与评分，再获取食物图片。
5.  **上菜与评价：** 
    *   文本生成完毕后，客人的对话框像打字机一样逐字输出评价。
    *   图片生成完毕后，在接待平台（屏幕中间）展示生成的像素风食物。
6.  **结算：** 弹出结算面板，显示星级 (0-5星)、扣除的食材成本、获得的报酬。更新玩家总资金。
7.  **判定：** 若资金 $\le 0$，触发破产结局；否则进入下一位客人/下一天。

## 4. UI 布局与页面结构
参考概念草图，采用经典的“对角线构图”：
*   **全局背景：** 深色调 (`dark background`)，像素风材质。
*   **头部 (Header)：** 显示当前状态（第 X 天）、金钱条（进度条或数字）、当前声望。
*   **场景区 (主视区)：**
    *   **横向接待台：** 贯穿屏幕中下部（1/3处）。
    *   **主角位置：** 接待台左下角。
    *   **客人位置：** 接待台右上角（自带对话气泡）。
    *   **食物展示位：** 位于接待台正中央，客人和主角之间。
*   **操作区 (底部面板)：**
    *   **食材标签页 (Tabs)：** 分为蔬菜、肉类、海鲜、调料等。以网格 (Grid) 形式展示图标。
    *   **厨具标签页：** 炒锅、烤箱、炖锅等。
    *   **已选列表：** 实时展示当前选中的食材组合。
    *   **烹饪按钮：** 醒目的行动按钮。

## 5. AI 系统核心需求与 Prompt 设计
系统必须通过 System Prompt 强制 LLM 以 `JSON` 格式返回数据。

**LLM 输入所需参数：**
*   `guest_story`: 客人刚才说的话。
*   `player_recipe`: 玩家选择的 [食材列表] + [厨具]。
*   `current_day`: 当前游戏天数（影响判定的严苛度）。

**LLM 预期输出 JSON 结构：**
```json
{
  "food_name": "String (料理名称)",
  "star_rating": "Number (0, 1, 2, 2.5, 3, 4, 5)",
  "evaluation": "String (客人的评价台词。如果是3-5星，语气治愈；如果是1-2星，必须使用《JOJO的奇妙冒险》风格的夸张比喻进行毒舌贬低；0星为食物中毒警告)",
  "image_prompt": "String (用于传给生图API的英文提示词，必须包含 'pixel art, 2d game asset, dark background, transparent background' 等限定词)"
}


6. 数值与经济系统 (MVP 版本)
初始资金： 1000 元。

单日客流： 每天固定接待 3 名客人。

难度递增： 随着天数增加，客人的要求从“直白菜名”过渡到“抽象情感”。

收益结算规则：

5 星：获得总成本的 300% 收入。

4 星：获得总成本的 200% 收入。

2.5 - 3 星：获得总成本的 100% 收入（保本）。

1 - 2 星：获得 0 收入（亏损食材费）。

0 星 (相克/中毒)：扣除 500 元医药费。

7. 开发任务拆解 (Phases)
请 Cursor 按照以下阶段依次执行：

Phase 1: 基础框架与静态 UI 搭建

初始化 Next.js + TailwindCSS + shadcn/ui 项目。

实现主游戏界面布局（Header 状态栏，中间场景区对角线构图，底部操作区网格）。

准备基础的静态 Mock 数据（几项默认食材、厨具）。

Phase 2: 核心状态管理与游戏循环

定义 TypeScript 接口 (GameState, Ingredient, GuestRequest 等)。

实现玩家点选食材、厨具的交互逻辑。

实现整个游戏 Loop 的状态机（等待点单 -> 点单中 -> 烹饪中 -> 结算中）。

（暂不接入 AI）使用一个假的 setTimeout 模拟烹饪过程并返回固定的 Mock JSON 结算数据。

Phase 3: 接入文本大模型 (LLM)

在 app/api/cook/route.ts 中编写后端接口，接入 LLM API。

完善 System Prompt，确保能够稳定输出 JSON。

前端接入该接口，实现“打字机效果”展示评价。

Phase 4: 接入图像生成与动效打磨

在后端逻辑中加入生图 API 调用，读取 LLM 生成的 image_prompt。

处理图片加载逻辑，将生成的食物图片展示在接待台上。

添加“烹饪中”的等待 CSS 动画，优化数值扣除/增加的视觉表现。